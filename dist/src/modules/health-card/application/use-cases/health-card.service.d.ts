import { IHealthCardRepository } from '../../domain/repositories/health-card.repository.interface';
import { IssueCardDto, CardActionDto, UpdateCardDto } from '../../presentation/dto/issue-card.dto';
import { FullHealthCardResponseDto, HealthCardHistoryItemDto } from '../../presentation/dto/full-health-card.dto';
import { OnboardHealthCardResponseDto } from '../../presentation/dto/onboard-health-card.dto';
import { VerifyQrDto, VerifyQrResponseDto } from '../../presentation/dto/verify-qr.dto';
import { Logger } from 'nestjs-pino';
import { QrService } from "../../../qr/application/use-cases/qr.service";
export declare class HealthCardService {
    private readonly repository;
    private readonly qrService;
    private readonly logger;
    constructor(repository: IHealthCardRepository, qrService: QrService, logger: Logger);
    private resolveProfile;
    private generateCardNumber;
    private mapCard;
    onboardHealthCard(userId: string): Promise<OnboardHealthCardResponseDto>;
    issueCard(userId: string, dto: IssueCardDto): Promise<FullHealthCardResponseDto>;
    getActiveCard(userId: string): Promise<FullHealthCardResponseDto>;
    getCardById(userId: string, cardId: string): Promise<FullHealthCardResponseDto>;
    updateCard(userId: string, cardId: string, dto: UpdateCardDto): Promise<FullHealthCardResponseDto>;
    activateCard(userId: string, cardId: string, dto: CardActionDto): Promise<FullHealthCardResponseDto>;
    deactivateCard(userId: string, cardId: string, dto: CardActionDto): Promise<FullHealthCardResponseDto>;
    suspendCard(userId: string, cardId: string, dto: CardActionDto): Promise<FullHealthCardResponseDto>;
    blockCard(userId: string, cardId: string, dto: CardActionDto): Promise<FullHealthCardResponseDto>;
    unblockCard(userId: string, cardId: string, dto: CardActionDto): Promise<FullHealthCardResponseDto>;
    replaceCard(userId: string, cardId: string, dto: CardActionDto): Promise<FullHealthCardResponseDto>;
    renewCard(userId: string, cardId: string, dto: CardActionDto): Promise<FullHealthCardResponseDto>;
    archiveCard(userId: string, cardId: string, dto: CardActionDto): Promise<FullHealthCardResponseDto>;
    restoreCard(userId: string, cardId: string, dto: CardActionDto): Promise<FullHealthCardResponseDto>;
    getCardHistory(userId: string, cardId: string): Promise<HealthCardHistoryItemDto[]>;
    searchCards(query: string): Promise<FullHealthCardResponseDto[]>;
    verifyQr(dto: VerifyQrDto, verifierUserId: string): Promise<VerifyQrResponseDto>;
    getCardDetails(userId: string): Promise<any>;
    getWalletPass(userId: string): Promise<any>;
}
